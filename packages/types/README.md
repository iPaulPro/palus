# Types

This package provides TypeScript definitions shared across the Palus monorepo.

## Commands

- `pnpm dev` – start development mode across workspaces.
- `pnpm build` – compile the monorepo packages.
